# ───────── Go ─────────
/rclone-sync
# Embedded frontend (rebuilt by `make web-dist`)
dist/web/
!dist/web/.gitkeep
# Build / cache
*.db
*.db-shm
*.db-wal
rcd.log
*.pid
vendor/

# ───────── Node (now under frontend/) ─────────
frontend/node_modules/

# ───────── Python (legacy — safe to keep until full history purge) ─────────
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ───────── Editors / OS ─────────
.vscode/
.DS_Store

# ───────── Env / secrets ─────────
.env
.env.local

# ───────── Release artifacts ─────────
dist/

# Local temp test data
*.tmp
